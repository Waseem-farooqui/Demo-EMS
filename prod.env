# Employee Management System - Production Environment Template
# This is a template for production deployment
# On production server: Copy this to .env and update with your actual values
# Command: cp prod.env .env
# Then edit .env with your production credentials

# ===================================
# Database Configuration
# ===================================
DB_NAME=employee_management_system
DB_USERNAME=emsuser
DB_PASSWORD=wud19@WUD
DB_ROOT_PASSWORD=wuf27@1991
DB_PORT=3306

# Optional: Override database URL (default uses 'mysql' container name for Docker)
# For Docker: Leave unset or use the default below (uses 'mysql' container name automatically)
# For non-Docker: Set to jdbc:mysql://localhost:3306/employee_management_system?...
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/employee_management_system?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true

# Optional: Override datasource username/password (defaults use DB_USERNAME and DB_PASSWORD above)
SPRING_DATASOURCE_USERNAME=emsuser
SPRING_DATASOURCE_PASSWORD=wud19@WUD

# ===================================
# JWT Configuration
# ===================================
# Generate a secure random string (at least 256 bits)
# You can use: openssl rand -base64 64
# IMPORTANT: Use a strong, unique secret in production!
JWT_SECRET=jqOe2xE/VdUM30B2h3hxB7bjjLF/Zofd4NVnH8Sm3u2nn7sHAMGmv4+yAsYJxwwF
JWT_EXPIRATION=86400000

# ===================================
# Email Configuration
# ===================================
# For Hostinger SMTP (Port 465 - SSL) - Recommended:
MAIL_HOST=smtp.hostinger.com
MAIL_PORT=465
MAIL_USERNAME=support@vertexdigitalsystem.com
MAIL_PASSWORD=wuf202019$WUF
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=false
MAIL_SMTP_STARTTLS_REQUIRED=false
MAIL_SMTP_SSL_ENABLE=true
MAIL_SMTP_SSL_TRUST=smtp.hostinger.com
MAIL_SMTP_SSL_SOCKET_FACTORY_PORT=465

# Alternative: For Hostinger SMTP (Port 587 - TLS/STARTTLS):
# MAIL_HOST=smtp.hostinger.com
# MAIL_PORT=587
# MAIL_USERNAME=support@vertexdigitalsystem.com
# MAIL_PASSWORD=CHANGE_THIS_TO_YOUR_HOSTINGER_EMAIL_PASSWORD
# MAIL_SMTP_AUTH=true
# MAIL_SMTP_STARTTLS_ENABLE=true
# MAIL_SMTP_STARTTLS_REQUIRED=true
# MAIL_SMTP_SSL_ENABLE=false
# MAIL_SMTP_SSL_TRUST=smtp.hostinger.com

# For Gmail (if using Gmail instead):
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password
# MAIL_SMTP_AUTH=true
# MAIL_SMTP_STARTTLS_ENABLE=true
# MAIL_SMTP_STARTTLS_REQUIRED=true
# MAIL_SMTP_SSL_ENABLE=false
# MAIL_SMTP_SSL_TRUST=smtp.gmail.com

EMAIL_FROM_NAME=VERTEX
EMAIL_FROM_ADDRESS=support@vertexdigitalsystem.com

# ===================================
# Application Configuration
# ===================================
# Production domain URL (use HTTPS in production)
APP_URL=https://vertexdigitalsystem.com
BACKEND_PORT=8080
FRONTEND_PORT=80

# ===================================
# CORS Configuration
# ===================================
# Comma-separated list of allowed origins
# IMPORTANT: Include all variations of your domain
# - HTTPS with and without www
# - HTTP with and without www (for testing/fallback)
CORS_ALLOWED_ORIGINS=https://vertexdigitalsystem.com,https://www.vertexdigitalsystem.com,http://vertexdigitalsystem.com,http://www.vertexdigitalsystem.com

# ===================================
# Frontend Build URLs (Required for Production)
# ===================================
# These URLs are used at build time to configure the frontend
# IMPORTANT: Use the same domain as frontend (without port 8080)
# Nginx proxies /api requests to backend internally
# For production, use your actual domain with HTTPS
API_URL=https://vertexdigitalsystem.com/api
API_BASE_URL=https://vertexdigitalsystem.com
FRONTEND_URL=https://vertexdigitalsystem.com

# ===================================
# OCR Configuration (Optional)
# ===================================
# OCR API Key for cloud OCR services (e.g., OCR.space, Google Vision API)
# Leave empty if using local Tesseract OCR only
OCR_API_KEY=

# Enable cloud OCR (requires OCR_API_KEY to be set)
OCR_CLOUD_ENABLED=false

# Enable local Tesseract OCR (recommended for most cases)
OCR_LOCAL_ENABLED=true

# Tesseract data path
TESSERACT_DATA_PATH=/usr/share/tesseract-ocr/5/tessdata
TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata

# ===================================
# Spring Profile
# ===================================
SPRING_PROFILES_ACTIVE=prod

# ===================================
# JPA Configuration
# ===================================
# IMPORTANT: Use 'validate' with Flyway (Flyway handles migrations)
# Do NOT use 'create' or 'update' in production with Flyway
JPA_DDL_AUTO=validate
JPA_SHOW_SQL=false
JPA_FORMAT_SQL=false
