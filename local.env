# Employee Management System - Local Development Environment
# This file is for local development on your machine
# Usage: docker compose --env-file local.env up

# ===================================
# Database Configuration
# ===================================
DB_NAME=employee_management_system
DB_USERNAME=emsuser
DB_PASSWORD=emspassword
DB_ROOT_PASSWORD=rootpassword
DB_PORT=3307

# ===================================
# JWT Configuration
# ===================================
# Generate a secure random string (at least 256 bits)
# You can use: openssl rand -base64 64
# For local development, you can use a simple secret (change in production!)
JWT_SECRET=local-development-secret-change-in-production-minimum-256-bits
JWT_EXPIRATION=86400000

# ===================================
# Email Configuration
# ===================================
# Gmail (for local testing)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password_here
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=true
MAIL_SMTP_STARTTLS_REQUIRED=true
MAIL_SMTP_SSL_ENABLE=false
MAIL_SMTP_SSL_TRUST=smtp.gmail.com

EMAIL_FROM_NAME=Employee Management System (Local)
EMAIL_FROM_ADDRESS=your_email@gmail.com

# ===================================
# Application Configuration
# ===================================
# Local development URL
APP_URL=http://localhost:4200
BACKEND_PORT=8080
FRONTEND_PORT=4200

# ===================================
# CORS Configuration
# ===================================
# Comma-separated list of allowed origins for local development
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200,http://localhost:80

# ===================================
# OCR Configuration
# ===================================
OCR_API_KEY=
OCR_CLOUD_ENABLED=false
OCR_LOCAL_ENABLED=true
TESSERACT_DATA_PATH=/usr/share/tesseract-ocr/5/tessdata

# ===================================
# Spring Profile
# ===================================
SPRING_PROFILES_ACTIVE=dev

# ===================================
# JPA Configuration
# ===================================
JPA_DDL_AUTO=validate
JPA_SHOW_SQL=false
JPA_FORMAT_SQL=false

